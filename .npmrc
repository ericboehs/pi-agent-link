# pi supplies typebox and @earendil-works/* to extensions as virtual modules
# (see its extension loader), so auto-installing our peers pulls ~180 MB into
# every clone that nothing ever imports. Skip them.
legacy-peer-deps=true
